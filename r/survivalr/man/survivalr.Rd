\name{survivalr}
\alias{survivalr}
\alias{survivalr-package}
\alias{Surv}
\alias{Surv2}
\alias{Surv2data}
\alias{aareg}
\alias{[.aareg}
\alias{labels.aareg}
\alias{lines.aareg}
\alias{plot.aareg}
\alias{print.aareg}
\alias{print.summary.aareg}
\alias{summary.aareg}
\alias{agexact.fit}
\alias{agreg.fit}
\alias{attrassign}
\alias{is.Surv}
\alias{is.ratetable}
\alias{[.ratetable}
\alias{as.matrix.ratetable}
\alias{is.na.ratetable}
\alias{Math.ratetable}
\alias{Ops.ratetable}
\alias{print.ratetable}
\alias{summary.ratetable}
\alias{ratetableDate}
\alias{ratetableDate.chron}
\alias{ratetableDate.date}
\alias{ratetableDate.Date}
\alias{ratetableDate.dates}
\alias{ratetableDate.default}
\alias{ratetableDate.integer}
\alias{ratetableDate.POSIXt}
\alias{ratetable}
\alias{[.ratetable2}
\alias{is.na.ratetable2}
\alias{match.ratetable}
\alias{cch}
\alias{coef.cch}
\alias{print.cch}
\alias{print.summary.cch}
\alias{summary.cch}
\alias{vcov.cch}
\alias{cipoisson}
\alias{clogit}
\alias{cluster}
\alias{blogit}
\alias{bprobit}
\alias{bcloglog}
\alias{blog}
\alias{aeqSurv}
\alias{tcut}
\alias{[.tcut}
\alias{levels.tcut}
\alias{neardate}
\alias{lvcf}
\alias{nostutter}
\alias{nsk}
\alias{makepredictcall.nsk}
\alias{ridge}
\alias{frailty}
\alias{frailty.gamma}
\alias{frailty.gaussian}
\alias{frailty.t}
\alias{untangle.specials}
\alias{pyears}
\alias{print.pyears}
\alias{summary.pyears}
\alias{pseudo}
\alias{pspline}
\alias{makepredictcall.pspline}
\alias{predict.pspline}
\alias{survfit}
\alias{survfitKM}
\alias{survfit0}
\alias{survfit_confint}
\alias{survSplit}
\alias{survcondense}
\alias{survcheck}
\alias{print.survcheck}
\alias{survdiff}
\alias{survexp}
\alias{finegray}
\alias{survobrien}
\alias{fromtimeline}
\alias{totimeline}
\alias{coxph}
\alias{coxph.control}
\alias{coxph.fit}
\alias{coxsurv.fit}
\alias{dsurvreg}
\alias{psurvreg}
\alias{qsurvreg}
\alias{rsurvreg}
\alias{royston}
\alias{rttright}
\alias{statefig}
\alias{strata}
\alias{survreg}
\alias{survreg.control}
\alias{survreg.distributions}
\alias{survreg.fit}
\alias{survregDtest}
\alias{basehaz}
\alias{brier}
\alias{concordance}
\alias{concordance.default}
\alias{concordance.survival_py_model}
\alias{concordancefit}
\alias{survConcordance}
\alias{survConcordance.fit}
\alias{cox.zph}
\alias{cox_zph}
\alias{coxph.detail}
\alias{coxph.wtest}
\alias{coxph_detail}
\alias{yates}
\alias{yates_setup}
\alias{yates_setup.coxph}
\alias{yates_setup.default}
\alias{yates_setup.glm}
\alias{yates_setup.survival_py_coxph}
\alias{survfitcoxph.fit}
\alias{survpenal.fit}
\alias{tmerge}
\alias{[.tmerge}
\alias{summary.tmerge}
\alias{survival_python_config}
\alias{survfit.character}
\alias{survfit.formula}
\alias{survfit.Surv}
\alias{survfit.survival_py_coxph}
\alias{survfit.survival_py_surv}
\alias{Math.Surv}
\alias{Ops.Surv}
\alias{Summary.Surv}
\alias{anyDuplicated.Surv}
\alias{as.character.Surv}
\alias{as.data.frame.Surv}
\alias{as.logical.Surv}
\alias{as.matrix.Surv}
\alias{barplot.Surv}
\alias{c.Surv}
\alias{density.Surv}
\alias{duplicated.Surv}
\alias{[.Surv}
\alias{format.Surv}
\alias{head.Surv}
\alias{hist.Surv}
\alias{identify.Surv}
\alias{image.Surv}
\alias{is.na.Surv}
\alias{length.Surv}
\alias{levels.Surv}
\alias{lines.Surv}
\alias{median.Surv}
\alias{names<-.Surv}
\alias{names.Surv}
\alias{pairs.Surv}
\alias{plot.Surv}
\alias{points.Surv}
\alias{print.Surv}
\alias{quantile.Surv}
\alias{rep.Surv}
\alias{rep.int.Surv}
\alias{rep_len.Surv}
\alias{rev.Surv}
\alias{t.Surv}
\alias{tail.Surv}
\alias{text.Surv}
\alias{unique.Surv}
\alias{xtfrm.Surv}
\alias{Math.Surv2}
\alias{Ops.Surv2}
\alias{Summary.Surv2}
\alias{anyDuplicated.Surv2}
\alias{as.character.Surv2}
\alias{as.data.frame.Surv2}
\alias{as.logical.Surv2}
\alias{as.matrix.Surv2}
\alias{c.Surv2}
\alias{duplicated.Surv2}
\alias{[.Surv2}
\alias{format.Surv2}
\alias{hist.Surv2}
\alias{identify.Surv2}
\alias{image.Surv2}
\alias{is.na.Surv2}
\alias{lines.Surv2}
\alias{pairs.Surv2}
\alias{points.Surv2}
\alias{print.Surv2}
\alias{rep.Surv2}
\alias{rep.int.Surv2}
\alias{rep_len.Surv2}
\alias{rev.Surv2}
\alias{t.Surv2}
\alias{tail.Surv2}
\alias{text.Surv2}
\alias{Math.survival_py_surv}
\alias{Ops.survival_py_surv}
\alias{Summary.survival_py_surv}
\alias{anyDuplicated.survival_py_surv}
\alias{as.character.survival_py_surv}
\alias{as.logical.survival_py_surv}
\alias{as.matrix.survival_py_surv}
\alias{barplot.survival_py_surv}
\alias{c.survival_py_surv}
\alias{density.survival_py_surv}
\alias{duplicated.survival_py_surv}
\alias{format.survival_py_surv}
\alias{head.survival_py_surv}
\alias{hist.survival_py_surv}
\alias{identify.survival_py_surv}
\alias{image.survival_py_surv}
\alias{is.na.survival_py_surv}
\alias{length.survival_py_surv}
\alias{levels.survival_py_surv}
\alias{lines.survival_py_surv}
\alias{median.survival_py_surv}
\alias{names<-.survival_py_surv}
\alias{names.survival_py_surv}
\alias{pairs.survival_py_surv}
\alias{plot.survival_py_surv}
\alias{points.survival_py_surv}
\alias{$.survival_py_survfit}
\alias{[[.survival_py_survfit}
\alias{[.survival_py_survfit}
\alias{aggregate.survival_py_survfit}
\alias{as.list.survival_py_survfit}
\alias{coef.survival_py_survfit}
\alias{confint.survival_py_survfit}
\alias{dim.survival_py_survfit}
\alias{length.survival_py_survfit}
\alias{lines.survival_py_survfit}
\alias{median.survival_py_survfit}
\alias{names.survival_py_survfit}
\alias{plot.survival_py_survfit}
\alias{points.survival_py_survfit}
\alias{quantile.survival_py_surv}
\alias{quantile.survival_py_survfit}
\alias{residuals.survival_py_survfit}
\alias{vcov.survival_py_survfit}
\alias{rep.survival_py_surv}
\alias{rep.int.survival_py_surv}
\alias{rep_len.survival_py_surv}
\alias{rev.survival_py_surv}
\alias{t.survival_py_surv}
\alias{tail.survival_py_surv}
\alias{text.survival_py_surv}
\alias{unique.survival_py_surv}
\alias{xtfrm.survival_py_surv}
\alias{[.survival_py_surv}
\alias{coef.survival_py_model}
\alias{vcov.survival_py_model}
\alias{confint.survival_py_model}
\alias{logLik.survival_py_model}
\alias{deviance.survival_py_model}
\alias{nobs.survival_py_model}
\alias{df.residual.survival_py_survreg}
\alias{extractAIC.survival_py_model}
\alias{formula.survival_py_model}
\alias{terms.survival_py_model}
\alias{labels.survival_py_model}
\alias{weights.survival_py_model}
\alias{model.matrix.survival_py_model}
\alias{model.frame.formula}
\alias{model.frame.survival_py_model}
\alias{model.frame.survival_py_survfit}
\alias{fitted.survival_py_model}
\alias{summary.survival_py_model}
\alias{summary.survival_py_survfit}
\alias{summary.survival_py_basehaz}
\alias{summary.survival_py_survdiff}
\alias{summary.survival_py_concordance}
\alias{summary.survival_py_cox_zph}
\alias{[.survival_py_cox_zph}
\alias{plot.survival_py_cox_zph}
\alias{summary.survival_py_coxph_detail}
\alias{summary.survival_py_anova}
\alias{coef.concordance}
\alias{vcov.concordance}
\alias{print.concordance}
\alias{coef.survival_py_concordance}
\alias{vcov.survival_py_concordance}
\alias{predict.survival_py_model}
\alias{residuals.survival_py_model}
\alias{anova.survival_py_model}
\alias{as.data.frame.survival_py_survfit}
\alias{as.data.frame.survival_py_surv}
\alias{as.data.frame.survival_py_basehaz}
\alias{as.data.frame.survival_py_survdiff}
\alias{as.data.frame.survival_py_concordance}
\alias{as.data.frame.survival_py_cox_zph}
\alias{as.data.frame.survival_py_coxph_detail}
\alias{as.data.frame.survival_py_anova}
\alias{print.survival_py_model}
\alias{print.survival_py_object}
\alias{print.survival_py_surv}
\alias{print.survival_py_survfit}
\alias{print.survival_py_basehaz}
\alias{print.survival_py_survdiff}
\alias{print.survival_py_concordance}
\alias{print.survival_py_cox_zph}
\alias{print.survival_py_coxph_detail}
\alias{print.survival_py_anova}
\alias{print.summary.survival_py_model}
\title{R bridge for the Rust-backed Python survival package}
\description{
Experimental R facade for the Rust-backed Python \pkg{survival} package.
The exported functions preserve familiar R survival-analysis entry points while
delegating computation to \code{survival.r_api} through \pkg{reticulate}.
}
\usage{
Surv(time, time2, event, type = NULL, origin = 0, time1, start, stop, status)

\method{Math}{Surv}(...)

\method{Ops}{Surv}(...)

\method{Summary}{Surv}(...)

\method{anyDuplicated}{Surv}(x, ...)

\method{as.character}{Surv}(x, ...)

\method{as.data.frame}{Surv}(x, row.names = NULL, optional = FALSE,
  make.names = TRUE, ...)

\method{as.logical}{Surv}(x, ...)

\method{as.matrix}{Surv}(x, ...)

\method{c}{Surv}(...)

\method{duplicated}{Surv}(x, ...)

\method{[}{Surv}(x, i, j, drop = FALSE)

\method{format}{Surv}(x, ...)

\method{head}{Surv}(x, ...)

\method{is.na}{Surv}(x)

\method{length}{Surv}(x)

\method{levels}{Surv}(x)

\method{median}{Surv}(x, na.rm = FALSE, ...)

\method{names}{Surv}(x) <- value

\method{names}{Surv}(x)

\method{print}{Surv}(x, quote = FALSE, ...)

\method{quantile}{Surv}(x, probs = c(0.25, 0.5, 0.75),
  na.rm = FALSE, ...)

\method{rep}{Surv}(x, ...)

\method{rep.int}{Surv}(x, ...)

\method{rep_len}{Surv}(x, ...)

\method{rev}{Surv}(x)

\method{t}{Surv}(x)

\method{tail}{Surv}(x, ...)

\method{unique}{Surv}(x, ...)

\method{xtfrm}{Surv}(x)

Surv2(time, event, repeated = FALSE)

\method{Math}{Surv2}(...)

\method{Ops}{Surv2}(...)

\method{Summary}{Surv2}(...)

\method{anyDuplicated}{Surv2}(x, ...)

\method{as.data.frame}{Surv2}(x, row.names = NULL, optional = FALSE,
  make.names = TRUE, ...)

\method{as.logical}{Surv2}(x, ...)

\method{as.matrix}{Surv2}(x, ...)

\method{c}{Surv2}(...)

\method{duplicated}{Surv2}(x, ...)

\method{[}{Surv2}(x, i, j, drop = FALSE)

\method{print}{Surv2}(x, quote = FALSE, ...)

\method{rep}{Surv2}(x, ...)

\method{rep.int}{Surv2}(x, ...)

\method{rep_len}{Surv2}(x, ...)

\method{rev}{Surv2}(x)

\method{t}{Surv2}(x)

\method{tail}{Surv2}(x, ...)

Surv2data(formula, data, subset, id)

aareg(formula, data, weights, subset, na.action, qrtol = 1e-07,
  nmin, dfbeta = FALSE, taper = 1,
  test = c("aalen", "variance", "nrisk"), cluster,
  model = FALSE, x = FALSE, y = FALSE)

\method{[}{aareg}(x, ..., drop = FALSE)

\method{labels}{aareg}(object, ...)

\method{lines}{aareg}(x, se = FALSE, maxtime, type = "s", ...)

\method{plot}{aareg}(x, se = TRUE, maxtime, type = "s", ...)

\method{print}{aareg}(x, maxtime, test = c("aalen", "nrisk"),
  scale = 1, ...)

\method{summary}{aareg}(object, maxtime, test = c("aalen", "nrisk"),
  scale = 1, ...)

\method{print}{summary.aareg}(x, ...)

attrassign(object, tt)

is.Surv(x)

is.ratetable(x, verbose = FALSE)

\method{[}{ratetable}(x, ..., drop = TRUE)

\method{as.matrix}{ratetable}(x, ...)

\method{is.na}{ratetable}(x)

\method{Math}{ratetable}(x, ...)

\method{Ops}{ratetable}(e1, e2)

\method{print}{ratetable}(x, ...)

\method{summary}{ratetable}(object, ...)

ratetableDate(x)

ratetable(...)

\method{[}{ratetable2}(x, rows, cols, drop = FALSE)

\method{is.na}{ratetable2}(x)

match.ratetable(R, ratetable)

cipoisson(k, time = 1, p = 0.95, method = c("exact", "anscombe"))

cluster(x)

blogit(edge = 0.05)

bprobit(edge = 0.05)

bcloglog(edge = 0.05)

blog(edge = 0.05)

aeqSurv(x, tolerance = sqrt(.Machine$double.eps))

lvcf(id, x, time)

nostutter(id, x, censor = 0, single = FALSE)

nsk(x, df = NULL, knots = NULL, intercept = FALSE, b = 0.05,
  Boundary.knots = stats::quantile(x, c(b, 1 - b), na.rm = TRUE))

\method{makepredictcall}{nsk}(var, call)

ridge(..., theta, df = nvar/2, eps = 0.1, scale = TRUE)

frailty(x, distribution = "gamma", ...)

frailty.gamma(x, sparse = (nclass > 5), theta, df, eps = 1e-05,
  method = c("em", "aic", "df", "fixed"), ...)

frailty.gaussian(x, sparse = (nclass > 5), theta, df,
  method = c("reml", "aic", "df", "fixed"), ...)

frailty.t(x, sparse = (nclass > 5), theta, df, eps = 1e-05,
  tdf = 5, method = c("aic", "df", "fixed"), ...)

pspline(x, df = 4, theta, nterm = 2.5 * df, degree = 3,
  eps = 0.1, method, Boundary.knots = range(x),
  intercept = FALSE, penalty = TRUE, combine, ...)

\method{makepredictcall}{pspline}(var, call)

\method{predict}{pspline}(object, newx, ...)

untangle.specials(tt, special, order = 1)

survfit(formula, ...)

\method{survfit}{formula}(formula, data = NULL, ..., subset = NULL,
  na.action = "fail")

\method{survfit}{character}(formula, data = NULL, ..., subset = NULL,
  na.action = "fail")

\method{survfit}{Surv}(formula, ..., group = NULL, subset = NULL,
  na.action = "fail")

\method{survfit}{survival_py_surv}(formula, ..., group = NULL, subset = NULL,
  na.action = "fail")

\method{survfit}{survival_py_coxph}(formula, newdata = NULL, ..., se.fit = TRUE)

\method{model.frame}{formula}(formula, ...)

survfitKM(x, y, weights = rep(1, length(x)), stype = 1, ctype = 1,
  se.fit = TRUE, conf.int = 0.95,
  conf.type = c("log", "log-log", "plain", "none", "logit", "arcsin"),
  conf.lower = c("usual", "peto", "modified"), start.time,
  id, cluster, robust, influence = FALSE, type, entry = FALSE,
  time0 = FALSE)

\method{as.character}{survival_py_surv}(x, ...)

\method{Math}{survival_py_surv}(...)

\method{Ops}{survival_py_surv}(...)

\method{Summary}{survival_py_surv}(...)

\method{anyDuplicated}{survival_py_surv}(x, ...)

\method{as.logical}{survival_py_surv}(x, ...)

\method{as.matrix}{survival_py_surv}(x, ...)

\method{c}{survival_py_surv}(...)

\method{duplicated}{survival_py_surv}(x, ...)

\method{format}{survival_py_surv}(x, ...)

\method{head}{survival_py_surv}(x, ...)

\method{is.na}{survival_py_surv}(x)

\method{length}{survival_py_surv}(x)

\method{levels}{survival_py_surv}(x)

\method{median}{survival_py_surv}(x, na.rm = FALSE, ...)

\method{names}{survival_py_surv}(x) <- value

\method{names}{survival_py_surv}(x)

\method{quantile}{survival_py_surv}(x, probs = c(0.25, 0.5, 0.75),
  na.rm = FALSE, ...)

\method{quantile}{survival_py_survfit}(x, probs = c(0.25, 0.5, 0.75),
  conf.int = TRUE, scale, tolerance = sqrt(.Machine$double.eps), ...)

\method{aggregate}{survival_py_survfit}(x, by = NULL, FUN = mean, ...)

\method{median}{survival_py_survfit}(x, ...)

\method{coef}{survival_py_survfit}(object, ...)

\method{vcov}{survival_py_survfit}(object, ...)

\method{confint}{survival_py_survfit}(object, ...)

\method{residuals}{survival_py_survfit}(object, times, type = "pstate",
  collapse = FALSE, weighted = collapse, data.frame = FALSE,
  extra = FALSE, ...)

\method{as.list}{survival_py_survfit}(x, ...)

\method{dim}{survival_py_survfit}(x)

\method{length}{survival_py_survfit}(x)

\method{plot}{survival_py_survfit}(x, ..., conf.int, mark.time = FALSE,
  pch = 3, col = 1, lty = 1, lwd = 1, cex = 1, log = FALSE,
  xscale = 1, yscale = 1, xlim, ylim, xmax, fun, xlab = "",
  ylab = "", xaxs = "r", cumhaz = FALSE)

\method{lines}{survival_py_survfit}(x, type = "s", pch = 3, col = 1,
  lty = 1, lwd = 1, cex = 1, mark.time = FALSE, xmax, fun,
  conf.int = FALSE, cumhaz = FALSE, xscale = 1, yscale = 1, ...)

\method{points}{survival_py_survfit}(x, fun, censor = FALSE, col = 1,
  pch, cumhaz = FALSE, xscale = 1, yscale = 1, ...)

\method{names}{survival_py_survfit}(x)

\method{[[}{survival_py_survfit}(x, i, ..., exact = TRUE)

\method{[}{survival_py_survfit}(x, i, j, k, ..., drop = TRUE)

\method{$}{survival_py_survfit}(x, name)

\method{rep}{survival_py_surv}(x, ...)

\method{rep.int}{survival_py_surv}(x, ...)

\method{rep_len}{survival_py_surv}(x, ...)

\method{rev}{survival_py_surv}(x)

\method{t}{survival_py_surv}(x)

\method{tail}{survival_py_surv}(x, ...)

\method{unique}{survival_py_surv}(x, ...)

\method{xtfrm}{survival_py_surv}(x)

\method{[}{survival_py_surv}(x, i, j, drop = FALSE)

survfit0(x, ...)

survfit_confint(p, se, logse = TRUE, conf.type, conf.int = 0.95,
  selow, ulimit = TRUE)

pseudo(fit, times, type, collapse = TRUE, data.frame = FALSE, ...)

survSplit(formula, data, subset, na.action = na.pass, cut,
  start = "tstart", id, zero = 0, episode, end = "tstop",
  event = "event", added)

survcondense(formula, data, subset, weights, na.action = na.pass,
  id, start = "tstart", end = "tstop", event = "event")

survdiff(formula, data = NULL, subset = NULL, na.action = "fail",
  rho = 0, timefix = TRUE, ..., group = NULL)

survexp(formula, data, weights, subset, na.action, rmap, times,
  method = c("ederer", "hakulinen", "conditional", "individual.h",
    "individual.s"),
  cohort = TRUE, conditional = FALSE, ratetable = NULL, scale = 1,
  se.fit, model = FALSE, x = FALSE, y = FALSE, time, age, year,
  sex = NULL)

pyears(formula, data, weights, subset, na.action, rmap, ratetable,
  scale = 365.25, expect = c("event", "pyears"), model = FALSE,
  x = FALSE, y = FALSE, data.frame = FALSE, time, start, stop,
  event = NULL, group = NULL)

\method{print}{pyears}(x, ...)

\method{summary}{pyears}(object, header = TRUE, call = header, n = TRUE,
  event = TRUE, pyears = TRUE, expected = TRUE, rate = FALSE,
  rr = expected, ci.r = FALSE, ci.rr = FALSE, totals = FALSE,
  legend = TRUE, vline = FALSE, vertical = TRUE, nastring = ".",
  conf.level = 0.95, scale = 1, ...)

finegray(formula, data, weights, subset, na.action = na.pass, etype,
  prefix = "fg", count = "", id, timefix = TRUE, tstart, tstop,
  ctime, cprob, extend, keep)

survobrien(formula, data, subset, na.action, transform,
  time, status, covariate, strata = NULL)

cch(formula, data, subcoh, id, stratum = NULL, cohort.size,
  method = c("Prentice", "SelfPrentice", "LinYing", "I.Borgan",
    "II.Borgan"), robust = FALSE)

clogit(formula, data, weights, subset, na.action,
  method = c("exact", "approximate", "efron", "breslow"), ...)

fromtimeline(formula, data, subset, id, repeated = FALSE,
  lvcf = TRUE, yname = c("tstart", "tstop", "status"))

tmerge(data1, data2, id, ..., tstart, tstop, options)

\method{[}{tmerge}(x, ..., drop = TRUE)

\method{summary}{tmerge}(object, ...)

totimeline(formula, data, id, istate)

royston(fit, newdata, ties = TRUE, adjust = FALSE)

survcheck(formula, data = NULL, subset = NULL, na.action = na.pass,
  id, istate, istate0 = "(s0)", timefix = TRUE, ...)

\method{print}{survcheck}(x, ...)

rttright(formula, data, weights, subset, na.action, times, id,
  timefix = TRUE, renorm = TRUE)

statefig(layout, connect, margin = 0.03, box = TRUE, cex = 1,
  col = 1, lwd = 1, lty = 1, bcol = col, acol = col,
  alwd = lwd, alty = lty, offset = 0)

strata(..., na.group = FALSE, shortlabel, sep = ", ")

coxph(formula, data = NULL, ..., subset = NULL, na.action = "fail")

coxph.control(eps = 1e-09, toler.chol = .Machine$double.eps^0.75,
  iter.max = 20, toler.inf = sqrt(eps), outer.max = 10,
  timefix = TRUE)

coxph.fit(x, y, strata, offset, init, control, weights, method,
  rownames, resid = TRUE, nocenter = NULL)

agexact.fit(x, y, strata, offset, init, control, weights, method,
  rownames, resid = TRUE, nocenter = NULL)

agreg.fit(x, y, strata, offset, init, control, weights, method,
  rownames, resid = TRUE, nocenter = NULL)

survreg(formula, data = NULL, ..., subset = NULL, na.action = "fail")

dsurvreg(x, mean, scale = 1, distribution = "weibull", parms)

psurvreg(q, mean, scale = 1, distribution = "weibull", parms)

qsurvreg(p, mean, scale = 1, distribution = "weibull", parms)

rsurvreg(n, mean, scale = 1, distribution = "weibull", parms)

survreg.control(maxiter = 30, rel.tolerance = 1e-09,
  toler.chol = 1e-10, iter.max, debug = 0, outer.max = 10)

survreg.distributions

survreg.fit(x, y, weights, offset, init, controlvals, dist,
  scale = 0, nstrat = 1, strata, parms = NULL, assign)

survpenal.fit(x, y, weights, offset, init, controlvals, dist,
  scale = 0, nstrat = 1, strata, pcols, pattr, assign,
  parms = NULL)

survregDtest(dlist, verbose = FALSE)

basehaz(fit, newdata, centered = TRUE, ...)

brier(fit, times, newdata, ties = TRUE, detail = FALSE,
  timefix = TRUE, efron = FALSE)

concordance(object, ..., formula)

\method{concordance}{default}(object, data = NULL, ..., scores = NULL, risk.scores = NULL,
  weights = NULL, cluster = NULL, subset = NULL, na.action = "fail")

\method{concordance}{survival_py_model}(object, ..., newdata, cluster, ymin,
  ymax, timewt = c("n", "S", "S/G", "n/G2", "I"), influence = 0,
  ranks = FALSE, timefix = TRUE, keepstrata = 10)

concordancefit(y, x, strata, weights, ymin = NULL, ymax = NULL,
  timewt = c("n", "S", "S/G", "n/G2", "I"), cluster,
  influence = 0, ranks = FALSE, reverse = FALSE, timefix = TRUE,
  keepstrata = 10, std.err = TRUE)

survConcordance(formula, data, weights, subset, na.action)

survConcordance.fit(y, x, strata, weight)

cox.zph(fit, transform = "km", terms = TRUE, singledf = FALSE,
  global = TRUE, ...)

cox_zph(fit, ...)

coxph.detail(object, riskmat = FALSE, rorder = c("data", "time"), ...)

coxph.wtest(var, b, toler.chol = 1e-09)

coxph_detail(object, ...)

coxsurv.fit(ctype, stype, se.fit, varmat, cluster, y, x, wt,
  risk, position, strata, oldid, y2, x2, risk2, strata2,
  id2, unlist = TRUE)

survfitcoxph.fit(y, x, wt, x2, risk, newrisk, strata, se.fit,
  survtype, vartype, varmat, id, y2, strata2, unlist = TRUE)

yates(fit, term, population = c("data", "factorial", "sas"), levels,
  test = c("global", "trend", "pairwise"), predict = "linear",
  options, nsim = 200, method = c("direct", "sgtt"))

yates_setup(fit, ...)

\method{yates_setup}{coxph}(fit, predict = c("lp", "risk", "expected",
  "terms", "survival", "linear"), options, ...)

\method{yates_setup}{default}(fit, type, ...)

\method{yates_setup}{glm}(fit, predict = c("link", "response", "terms",
  "linear"), ...)

\method{yates_setup}{survival_py_coxph}(fit, predict = c("lp", "risk",
  "expected", "terms", "survival", "linear"), options, ...)

survival_python_config()

tcut(x, breaks, labels, scale = 1)

\method{[}{tcut}(x, ..., drop = FALSE)

\method{levels}{tcut}(x)

neardate(id1, id2, y1, y2, best = c("after", "prior"),
  nomatch = NA_integer_)

\method{coef}{survival_py_model}(object, ...)

\method{vcov}{survival_py_model}(object, ..., complete = TRUE)

\method{confint}{survival_py_model}(object, parm, level = 0.95, ...)

\method{logLik}{survival_py_model}(object, ...)

\method{deviance}{survival_py_model}(object, ...)

\method{nobs}{survival_py_model}(object, ...)

\method{df.residual}{survival_py_survreg}(object, ...)

\method{extractAIC}{survival_py_model}(fit, scale = 0, k = 2, ...)

\method{formula}{survival_py_model}(x, ...)

\method{terms}{survival_py_model}(x, ...)

\method{labels}{survival_py_model}(object, ...)

\method{weights}{survival_py_model}(object, ...)

\method{model.matrix}{survival_py_model}(object, ...)

\method{model.frame}{survival_py_model}(formula, ...)

\method{model.frame}{survival_py_survfit}(formula, ...)

\method{fitted}{survival_py_model}(object, ..., type = NULL, se.fit = FALSE)

\method{summary}{survival_py_model}(object, conf.int = 0.95, scale = 1, ...)

\method{summary}{survival_py_survfit}(object, times, censored = FALSE,
  scale = 1, extend = FALSE, rmean = getOption("survfit.rmean"),
  data.frame = FALSE, dosum, ...)

\method{summary}{survival_py_basehaz}(object, ...)

\method{summary}{survival_py_survdiff}(object, ...)

\method{summary}{survival_py_concordance}(object, ...)

\method{summary}{survival_py_cox_zph}(object, ...)

\method{[}{survival_py_cox_zph}(x, ..., drop = FALSE)

\method{plot}{survival_py_cox_zph}(x, resid = TRUE, se = TRUE, df = 4,
  nsmo = 40, var, xlab = "Time", ylab = "", lty = 1:2, col = 1,
  lwd = 1, pch = 1, cex = 1, hr = FALSE, plot = TRUE, ...)

\method{summary}{survival_py_coxph_detail}(object, ...)

\method{summary}{survival_py_anova}(object, ...)

\method{coef}{concordance}(object, ...)

\method{vcov}{concordance}(object, ...)

\method{coef}{survival_py_concordance}(object, ...)

\method{vcov}{survival_py_concordance}(object, ...)

\method{predict}{survival_py_model}(object, newdata = NULL, ..., type = NULL,
  se.fit = FALSE)

\method{residuals}{survival_py_model}(object, ..., type = "martingale")

\method{anova}{survival_py_model}(object, ..., test = "Chisq")

\method{as.data.frame}{survival_py_survfit}(x, row.names = NULL,
  optional = FALSE, ...)

\method{as.data.frame}{survival_py_surv}(x, row.names = NULL,
  optional = FALSE, ...)

\method{as.data.frame}{survival_py_basehaz}(x, row.names = NULL,
  optional = FALSE, ...)

\method{as.data.frame}{survival_py_survdiff}(x, row.names = NULL,
  optional = FALSE, ...)

\method{as.data.frame}{survival_py_concordance}(x, row.names = NULL,
  optional = FALSE, ...)

\method{as.data.frame}{survival_py_cox_zph}(x, row.names = NULL,
  optional = FALSE, ...)

\method{as.data.frame}{survival_py_coxph_detail}(x, row.names = NULL,
  optional = FALSE, ...)

\method{as.data.frame}{survival_py_anova}(x, row.names = NULL,
  optional = FALSE, ...)

\method{print}{concordance}(x, digits = max(1L, getOption("digits") - 3L), ...)

\method{print}{survival_py_model}(x, ...)

\method{print}{survival_py_object}(x, ...)

\method{print}{survival_py_surv}(x, ...)

\method{print}{survival_py_survfit}(x, ...)

\method{print}{survival_py_basehaz}(x, ...)

\method{print}{survival_py_survdiff}(x, ...)

\method{print}{survival_py_concordance}(x, ...)

\method{print}{survival_py_cox_zph}(x, ...)

\method{print}{survival_py_coxph_detail}(x, ...)

\method{print}{survival_py_anova}(x, ...)

\method{print}{summary.survival_py_model}(x,
  digits = max(getOption("digits") - 3, 3),
  signif.stars = getOption("show.signif.stars"), expand = FALSE, ...)
}
\arguments{
\item{time, time2, event}{Survival response vectors. \code{time} is also
accepted as exposure time for \code{cipoisson}, a direct \code{survexp}
follow-up vector, and direct \code{survobrien} event time vector.}
\item{time1, start, stop, status}{Named aliases for R-style response
construction. \code{time1} and \code{start} map to the first time column,
\code{stop} maps to \code{time2}, and \code{status} maps to \code{event};
\code{status} is also the direct event-code vector for \code{survobrien}.}
\item{type}{Optional survival response, prediction, \code{survfitKM} curve,
plotted line, residual, or pseudo-value type.}
\item{origin}{Optional time origin passed to the Python response constructor.}
\item{value}{Object to test.}
\item{verbose}{Logical flag accepted by \code{is.ratetable}.}
\item{e1, e2}{Operands for rate-table arithmetic and comparisons.}
\item{edge}{Lower/upper probability clamp for bounded link helpers
\code{blogit}, \code{bprobit}, \code{bcloglog}, and \code{blog}.}
\item{tolerance}{Near-tie tolerance for \code{aeqSurv} and survival
quantile inversion.}
\item{breaks}{Strictly increasing cut points for \code{tcut}.}
\item{labels}{Optional interval labels for \code{tcut}.}
\item{cut}{Cut points for \code{survSplit}.}
\item{repeated}{Logical flag or \code{"first"} accepted by \code{Surv2}
and timeline conversion for repeated events.}
\item{lvcf}{Logical flag controlling last-value-carried-forward handling in
\code{fromtimeline}.}
\item{yname}{Optional two- or three-element output response names for
\code{fromtimeline}.}
\item{id1}{Query IDs for \code{neardate}.}
\item{id2}{Reference IDs for \code{neardate}, or second subject identifiers
for low-level Cox survival-curve inputs.}
\item{y1, y2}{Query and reference dates or numeric values for \code{neardate}.}
\item{best}{Nearest-date direction, either \code{"after"} or \code{"prior"}.
The bridge also accepts \code{"closest"} as a Python-backed extension.}
\item{nomatch}{Scalar value used when \code{neardate} cannot find a match.}
\item{df}{Optional degrees of freedom for \code{nsk}, \code{ridge},
\code{frailty.*}, \code{pspline}, and the \code{cox.zph} plot smoother.}
\item{knots}{Optional interior knot locations for \code{nsk}.}
\item{intercept}{Logical flag controlling whether \code{nsk} includes the
first knot-height column, or whether \code{pspline} includes an intercept.}
\item{b}{Boundary quantile used by the default \code{nsk} boundary knots.}
\item{Boundary.knots}{Boundary knots for \code{nsk} and \code{pspline};
logical values follow the compatibility behavior of \code{survival::nsk}.}
\item{call}{Model call updated with fitted spline metadata for prediction,
or a logical control for showing the fitted call in a \code{pyears} summary.}
\item{newx}{New predictor values for \code{predict.pspline}.}
\item{theta}{Fixed penalty parameter for \code{ridge}, \code{frailty.*}, and
\code{pspline}.}
\item{sparse}{Logical flag controlling sparse frailty coding.}
\item{tdf}{Degrees of freedom for Student-t frailty.}
\item{nterm, degree, penalty, combine}{Spline basis and penalty controls for
\code{pspline}.}
\item{tstart, tstop, ctime, cprob, extend, keep}{Direct-vector inputs for
Python-backed \code{finegray} interval expansion. On the first \code{tmerge}
call, \code{tstart} and \code{tstop} establish each subject's observation
range. \code{extend} also controls whether
\code{summary.survival_py_survfit} carries estimates beyond the last observed
curve time.}
\item{data1, data2}{Input data frames for native \code{tmerge} interval construction.}
\item{options}{Named \code{tmerge} controls: \code{idname},
\code{tstartname}, \code{tstopname}, \code{delay}, \code{na.rm}, and
\code{tdcstart}; also optional prediction settings for \code{yates}.}
\item{subcoh, stratum, cohort.size}{Case-cohort design inputs for native
\code{cch} fitting. Unstratified Prentice, Self-Prentice, and Lin--Ying
methods and sampling-stratified I.Borgan and II.Borgan methods are supported.}
\item{qrtol, nmin, dfbeta, taper}{Control values for Rust-backed
\code{aareg} risk-set fitting and influence diagnostics.}
\item{id}{Optional generated row identifier column for \code{survSplit}, or
subject identifiers for \code{survcondense}, \code{survcheck}, timeline
converters, \code{Surv2data}, native \code{cch}, native \code{tmerge}, and
low-level \code{survfitKM}.}
\item{istate}{Optional initial state vector for \code{survcheck} and
timeline converters.}
\item{istate0}{Initial-state label accepted for \code{survcheck}
compatibility.}
\item{zero}{Origin used when splitting right-censored \code{survSplit}
input.}
\item{censor, single}{Event-censor code and singleton handling for
\code{nostutter}. \code{censor} also controls whether
\code{points.survival_py_survfit} marks censoring rows instead of event rows.}
\item{episode, end, added}{Output column controls for
\code{survSplit} and \code{survcondense}; \code{added} is accepted for R API
compatibility.}
\item{tt, special, order}{Term-object inputs for \code{attrassign} and
special-term inputs for \code{untangle.specials}.}
\item{formula}{Formula, formula string, bridged \code{Surv} response, or
bridged fitted Cox model accepted by \code{survival.r_api}.}
\item{data, newdata}{Optional data frames or Python-compatible data objects.}
\item{subset}{Optional row subset.}
\item{na.action}{Missing-data policy, either a string or one of the common
\pkg{stats} NA action functions.}
\item{na.rm}{Logical flag controlling whether missing \code{Surv} rows are
removed before computing survival quantiles.}
\item{group}{Optional group vector for direct \code{survdiff} and
\code{pyears} calls. Python \code{pyears} also accepts a \code{tcut} result
for a time-changing group.}
\item{by}{Optional grouping vector or list for aggregating Python-backed Cox
\code{survfit} prediction curves across their \code{data} margin.}
\item{FUN}{Summary function for \code{aggregate.survival_py_survfit};
currently \code{mean} is supported.}
\item{rmean, dosum}{Accepted \code{summary.survival_py_survfit}
compatibility arguments. The tabular bridge summary uses \code{dosum} for
interval event/censor counts when explicit \code{times} are supplied.}
\item{rmap}{Optional rate-table variable mapping for formula
\code{survexp} and \code{pyears} calls delegated to \pkg{survival}.}
\item{expect}{Expected-count scale selector accepted by \code{pyears}.}
\item{header}{Logical control for the heading in a \code{pyears} summary.}
\item{pyears, expected, rate, rr, ci.r, ci.rr}{Logical controls for the
exposure, expected-event, rate, observed-to-expected, and confidence interval
columns in a \code{pyears} summary. The \code{n} and \code{event} arguments
likewise control their count columns.}
\item{totals}{Logical flag adding marginal totals to a \code{pyears} summary.}
\item{legend}{Logical compatibility flag accepted by \code{summary.pyears}.}
\item{vline, vertical}{Logical controls for table borders and orientation in a
\code{pyears} summary.}
\item{nastring}{Text used for missing cells in a \code{pyears} summary.}
\item{conf.level}{Confidence level for \code{pyears} rate and
observed-to-expected intervals.}
\item{method}{Expected-survival method for \code{survexp}, tie method for
\code{coxph.fit} and \code{clogit}, case-cohort method for \code{cch},
penalty selection method for \code{frailty.*} and \code{pspline}, or interval method
for \code{cipoisson}. Direct Python \code{survexp} calls accept
\code{"ederer"}, \code{"hakulinen"}, \code{"conditional"},
\code{"individual.h"}, and \code{"individual.s"}. For \code{yates}, method
selects the direct or SGTT contrast path.}
\item{etype, prefix, count}{Formula \code{finegray} endpoint and output-column
options handled by the native bridge.}
\item{transform}{Formula \code{survobrien} option delegated to
\code{survival::survobrien}, or the time-axis transform for \code{cox.zph}.}
\item{terms, singledf, global}{Logical controls for term grouping,
single-degree-of-freedom tests, and the global test in \code{cox.zph}.}
\item{nsmo, hr, plot}{Number of smoothing-grid points, hazard-ratio display
flag, and drawing flag for \code{plot.survival_py_cox_zph}. Set
\code{plot = FALSE} to return the smoothed curve data.}
\item{covariate}{Direct numeric covariate vector for Python-backed
\code{survobrien}.}
\item{scores, risk.scores}{Optional direct concordance score vectors or
matrices. Use at most one of these.}
\item{weight, weights}{Optional case weights.}
\item{R}{Rate-table covariate matrix or data frame for
\code{match.ratetable}.}
\item{ratetable}{Optional Python-backed rate table for direct
\code{survexp} calls, or a native R reference table for
\code{match.ratetable}. Omit it in direct \code{survexp} calls to use the
bundled Python US table. Formula calls are delegated to
\code{survival::survexp}.}
\item{times}{Optional \code{rttright} output times or requested
\code{summary.survival_py_survfit} times. Multiple \code{rttright} times
return a numeric matrix with one row per input observation and one column per
time.}
\item{cohort, conditional}{Logical flags selecting the default direct
\code{survexp} method when \code{method} is omitted.}
\item{ties, adjust}{Logical options for \code{royston}; \code{ties}
controls tied linear-predictor normal scores and \code{adjust} applies the
Royston-Sauerbrei small-sample adjustment.}
\item{renorm}{Logical flag controlling \code{rttright} weight normalization.}
\item{layout, connect}{State layout specification and square transition
matrix for \code{statefig}.}
\item{margin, box, cex, col, lwd, lty, bcol, acol, alwd, alty}{Plot
style arguments accepted for \code{statefig} compatibility.}
\item{pch, log, xscale, yscale, xlim, ylim, xmax, xlab, ylab, xaxs,
mark.time, cumhaz, fun}{Plotting controls for Python-backed
\code{survfit} curves. The bridge supports common step-curve drawing and
basic survival/cumulative-hazard transformations.}
\item{na.group}{Logical flag controlling whether missing \code{strata}
values form an explicit \code{NA} level.}
\item{shortlabel}{Logical flag controlling whether \code{strata} omits
variable-name prefixes from level labels.}
\item{sep}{Separator used when constructing multi-variable \code{strata}
labels.}
\item{cluster}{Optional cluster labels for robust variance where supported.}
\item{strata}{Optional strata labels for \code{coxph.fit},
\code{concordancefit}, \code{survConcordance.fit}, and direct
\code{survobrien}.}
\item{ymin, ymax, timewt, influence, ranks, reverse, keepstrata, std.err}{
Low-level \code{concordancefit} controls matching the R \pkg{survival}
entry point.}
\item{fit, object}{Bridged model or result object.}
\item{model}{Logical flag accepted for R formula compatibility where
supported.}
\item{x, y}{Logical flags accepted for R formula compatibility where
supported. \code{x} is also used as a data vector for helpers such as
\code{tcut}, \code{nsk}, \code{dsurvreg}, \code{concordancefit}, and
\code{survConcordance.fit}, and as the covariate matrix for
\code{coxph.fit}; \code{y} is also accepted as a \code{Surv} response for
\code{coxph.fit}, \code{concordancefit}, and \code{survConcordance.fit}. In
Python, \code{x} is the input vector for bounded link helper transforms.}
\item{age, year, sex}{Direct-vector rate-table inputs for Python-backed
\code{survexp}.}
\item{censored}{Logical flag controlling whether \code{summary.survival_py_survfit}
includes censor-only rows when no explicit \code{times} vector is supplied.}
\item{collapse, weighted, extra}{Logical flags for \code{pseudo} and
\code{residuals.survival_py_survfit}. The residuals bridge supports
right-censored, counting-process, and multi-state \code{survfit} objects with
retained model frames. Multi-state results preserve state or transition array
dimensions across requested times.}
\item{data.frame}{Logical flag controlling whether \code{pseudo} returns a
long data frame instead of a matrix.}
\item{q, p, n}{Quantiles, probabilities, or draw count for the
\code{survreg} distribution helpers; \code{p} is the confidence level for
\code{cipoisson} and the survival probabilities for
\code{survfit_confint}.}
\item{probs}{Survival quantile probabilities.}
\item{se}{Standard-error vector for \code{survfit_confint}, or logical flag
controlling confidence curves for \code{aareg} and \code{cox.zph} plots.}
\item{logse}{Logical flag indicating whether \code{se} is already on the
log-survival scale for \code{survfit_confint}.}
\item{conf.type}{Confidence interval transform for \code{survfit_confint}
and \code{survfitKM}.}
\item{conf.int}{Confidence level for \code{survfit_confint} and Cox model
summaries, or logical flag controlling confidence bounds in survival
quantiles and Cox summaries.}
\item{selow}{Optional lower-side standard-error vector for asymmetric
\code{survfit_confint} bounds.}
\item{ulimit}{Logical flag controlling upper-bound clipping in
\code{survfit_confint}.}
\item{mean}{Location parameter for the \code{survreg} distribution helpers.}
\item{distribution}{Distribution name for \code{frailty} or the
\code{survreg} distribution helpers.}
\item{parms}{Optional distribution parameters. Used for the Student-t
\code{survreg} helper distribution; ignored by the other built-in helper
distributions to match R.}
\item{eps, toler.chol, iter.max, toler.inf, outer.max, timefix}{Control
options passed through \code{coxph.control}. \code{timefix} also controls
near-tie correction in \code{survdiff}. \code{eps} is also the
convergence tolerance used when \code{ridge}, \code{frailty.*}, or
\code{pspline} searches for a penalty matching \code{df}.}
\item{maxiter, rel.tolerance, debug}{Control options passed through
\code{survreg.control}.}
\item{dlist}{Distribution-list object for \code{survregDtest}.}
\item{row.names, optional, make.names}{Standard \code{as.data.frame}
arguments.}
\item{digits}{Number of significant digits used by concordance and model
summary printers.}
\item{signif.stars}{Logical flag controlling significance stars in printed
Cox coefficient tables.}
\item{expand}{Logical multi-state Cox print expansion flag, accepted for
compatibility.}
\item{quote}{Logical flag controlling whether printed survival-time labels are
quoted.}
\item{i, j, rows, cols, name, exact, drop}{Row and column subset controls
for native \code{Surv}, \code{Surv2}, and \code{ratetable2} objects, plus
strata, data, and state subset and list-field extraction controls for
Python-backed \code{survfit} objects.}
\item{centered}{Logical flag for centered baseline hazards.}
\item{rho}{Fleming--Harrington weight exponent for \code{survdiff}.}
\item{riskmat, rorder}{Logical risk-matrix request and data/time row-order
selector for \code{coxph.detail}.}
\item{detail}{Logical flag requesting detailed \code{brier} output.}
\item{efron}{Logical flag selecting Efron-compatible \code{brier} baseline
survival when the fitted Cox model used Efron ties.}
\item{term}{Model term for \code{yates}.}
\item{population, levels}{Population weighting and level controls for
\code{yates}.}
\item{predict, nsim}{Prediction scale and simulation count for \code{yates}.}
\item{complete}{Logical flag requesting the full variance matrix where the
Python fit exposes extra parameters such as estimated survreg scale terms.}
\item{offset, init, control, rownames, resid, nocenter}{Low-level
\code{coxph.fit}, \code{agexact.fit}, \code{agreg.fit}, native built-in
\code{survreg.fit}, and delegated \code{survpenal.fit} optimizer inputs matching the R
\pkg{survival} entry points. \code{offset} is also accepted by
\code{statefig}.}
\item{controlvals, dist, nstrat, pcols, pattr, assign}{Low-level
\code{survreg.fit} and \code{survpenal.fit} inputs. Built-in extreme-value,
logistic, Gaussian, and Student-t \code{survreg.fit} distributions use the
Rust optimizer; custom density callbacks retain the upstream fallback.}
\item{varmat, wt, risk, newrisk, position, oldid, x2, risk2, strata2,
survtype, vartype, unlist}{Low-level Cox survival-curve inputs.}
\item{var}{Variance matrix for \code{coxph.wtest}, a constructed spline
basis passed to a \code{makepredictcall} method, or variables selected for a
\code{cox.zph} plot.}
\item{parm}{Optional coefficient names or 1-based coefficient indices.}
\item{level}{Confidence level for coefficient intervals.}
\item{se.fit}{Logical flag requesting standard errors when supported.}
\item{stype, ctype, conf.lower, start.time, entry, time0, robust}{Low-level
\code{survfitKM} controls matching the R \pkg{survival} entry point where
supported.}
\item{scale}{Scale parameter for \code{survreg} distribution helpers, Cox
summary coefficients, the standard \code{extractAIC} scale argument, divisor
for survival quantile and \code{summary.survival_py_survfit} times, or a
logical flag controlling whether \code{ridge} scales the penalty by column
variance. It also scales \code{aareg} summary coefficients.}
\item{maxtime}{Optional upper time limit for \code{aareg} summaries and
cumulative-coefficient plots.}
\item{k}{Penalty multiplier for \code{extractAIC}, observed Poisson event
count for \code{cipoisson}, or the state-dimension selector for stratified
model-based multi-state \code{survfit} curves.}
\item{test}{Test name for bridged model analysis of variance, \code{aareg}
test selector, or contrast test selector for \code{yates}.}
\item{...}{Additional arguments passed to the Python API. For \code{tmerge},
these are named \code{tdc}, \code{cumtdc}, \code{event}, or \code{cumevent}
expressions evaluated against \code{data2}.}
}
\value{
A reticulate-wrapped Python object, an R-converted Python result, or a scalar
value depending on the delegated Python function.
}
\details{
This package is intentionally named \pkg{survivalr} so it can coexist with
CRAN's \pkg{survival} package while the bridge matures. The Python package must
be installed in the Python environment selected by \pkg{reticulate}.
\code{tmerge} builds `(tstart, tstop]` intervals with native linear-time
sweeps. It follows R boundary and tie rules, retains event censor values and
time-dependent covariate names, and appends all nine \code{tcount}
classification columns across repeated calls.

\code{cch} formulas are evaluated in R, expanded with \code{model.matrix}, and
fitted by the Rust Cox and case-cohort variance kernels. Sampling-stratified
I.Borgan and II.Borgan fits retain stratum sizes, optimal allocation fractions,
phase-two score matrices, and collapsed scores. The returned \code{cch} object
carries coefficient, model-based, phase-two, and final variance components with
R-compatible names and methods.

\code{survobrien} formula expansion retains factor keeper columns, including
their level ordering, while transforming continuous terms within each event
risk set.

Formula \code{finegray} calls use the Python formula engine and Rust interval
expansion directly. The bridge supports weights, subsets, missing-data actions,
strata, counting-process histories, and delayed entry; computes censoring risk
sets with sorted sweeps; and restores factor and \code{I(...)} column classes
on the returned data frame.

\code{coxsurv.fit} and \code{survfitcoxph.fit} use a Rust risk-set sweep for
weighted right-censored and counting-process baselines, including strata and
tied events. R-side assembly retains compatible curve, cumulative-hazard,
standard-error, and individual-trajectory output shapes.

Built-in \code{survreg.fit} matrix calls use positive-definite
observed-information steps with an outer-product fallback. Fixed and
stratified scales, left and interval censoring, null likelihoods, covariance,
scores, and linear predictors are returned in the usual low-level R shape.
}
\seealso{
\code{\link{survival_python_config}}
}
\keyword{survival}
\keyword{models}
